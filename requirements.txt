# --- Web framework ---------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Configuration ---------------------------------------------------------
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1

# --- Database --------------------------------------------------------------
SQLAlchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# --- LLM -------------------------------------------------------------------
# Groq is reached over its OpenAI-compatible REST endpoint using httpx (below),
# so no provider SDK is needed here.

# --- Google Sheets ---------------------------------------------------------
google-auth==2.37.0
google-api-python-client==2.156.0

# --- HTTP / utils ----------------------------------------------------------
httpx==0.28.1
tenacity==9.0.0
python-dateutil==2.9.0.post0

# IANA timezone database - not present on Windows or in slim Linux images,
# and `zoneinfo` needs it to resolve Asia/Kolkata.
tzdata==2024.2
