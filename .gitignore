__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

.env
.env.*
# Templates carry no secrets and belong in the repo.
!.env.example
!.env.production.example
secrets/*
!secrets/.gitkeep
*.pem
service-account*.json

.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
*.db
*.sqlite3

.idea/
.vscode/
.DS_Store

# Cloud Run configuration. Holds live credentials in plain text - the OpenAI
# key, the WhatsApp token, the database password and the console session
# secret. GitHub's secret scanning would block the push, and every value in it
# would need rotating. Copy it between machines by hand.
env.yaml
sa.json

# Claude Code's per-machine settings. Never committed: approving a command
# writes that command into this file verbatim, so any secret passed inline on a
# command line ends up here. That is exactly what GitHub push protection caught.
.claude/settings.local.json

# Frontend build output and dependencies
frontend/node_modules/
frontend/dist/
frontend/tsconfig.tsbuildinfo
